<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Clientes</title>
    <link rel="stylesheet" href="/css/styles3.css">
</head>

<body>
    <main>
        <h1>Lista de Clientes</h1>
        
        <table border="1" width="100%">
            <tr>
                <th>codcliente</th>
                <th>Nome</th>
                <th>Sobrenome</th>
                <th>Email</th>
                <th>WhatsApp</th>
                <th>CEP</th>
                <th>Logradouro</th>
                <th>Número</th>
                <th>Complemento</th>
                <th>Bairro</th>
                <th>Cidade</th>
                <th>Estado</th>
                <th>Alterar</th>
            </tr>

            <% if (cliente.length > 0) { %>
                
                <% for (var i = 0; i < cliente.length; i++) { %>
                    <tr>
                        <td><%= cliente[i].codcliente %></td> 
                        <td><%= cliente[i].nome %></td> 
                        <td><%= cliente[i].sobrenome %></td> 
                        <td><%= cliente[i].email %></td> 
                        <td><%= cliente[i].whatsapp %></td> 
                        <td><%= cliente[i].cep %></td> 
                        <td><%= cliente[i].logradouro %></td> 
                        <td><%= cliente[i].numero %></td> 
                        <td><%= cliente[i].complemento %></td> 
                        <td><%= cliente[i].bairro %></td> 
                        <td><%= cliente[i].cidade %></td> 
                        <td><%= cliente[i].estado %></td> 
                        <td><a href="/update-cliente?codcliente= <%- cliente[i].codcliente %>">Alterar</a></td>

                    </tr>
                <% } %>
            <% } else { %>
                <h1 class="empty-message">Não temos clientes cadastrados no banco de dados!</h1>
            <% } %>
        </table>
    </main>
</body>

</html>